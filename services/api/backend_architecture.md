Backend Architecture & Data Engineering Reference (local copy)

This is a per-repo copy intended for backend engineers and CI validation.

Key pointers:
- Implement primitives under services/backend/engine
- Tests live under services/api/tests
- Run: pip install -r services/api/requirements.txt && python -m pytest -q
